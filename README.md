# FBUNavigationExample

Simple iOS application that shows different types of navigation (push vs modal), and shows how to pass data between view controllers using a delegate, closure, or NSNotificationCenter
